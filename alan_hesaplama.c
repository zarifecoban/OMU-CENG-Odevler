#include <stdio.h>
int main(){

    float boy,en,cevre,alan;


    printf("dıktortgenın uzunlugunu gırınız : ");
    scanf("%f", &boy);
    printf("dıkdortgenın genıslıgını gırınız: ");
    scanf("%f", &en);

    cevre=2*(boy+en);
    alan= boy*en;
    printf("dıkdortgenın cevresı = %f\n bırımdır",cevre);
    printf("dıkdortgenın alanı = %f bırım karedir", alan);

}
